## 1. Archive p5 changes

- [ ] 1.1 `openspec archive p5-c001-project-store --yes` (folds its delta into `specs/`).
- [ ] 1.2 `openspec archive p5-c002-forge-rest-path-fix --yes`.
- [ ] 1.3 `openspec archive p5-c003-security-debt-closure --yes`.
- [ ] 1.4 `openspec archive p5-c004-integration-proof --yes`.

## 2. Verification

- [ ] 2.1 `openspec list` shows the four p5 changes as archived; `openspec/specs/` contains their capabilities.
- [ ] 2.2 `git status` shows only `openspec/` moves (no Rust source touched).
